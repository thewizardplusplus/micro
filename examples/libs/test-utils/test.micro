let ansi:2 load "../cli-utils/ansi";

fn test(name test)
	let result test;
	outln
		+ ansi "bold" name
		+ " "
		if result
			fn() ansi "green" "[SUCCESS]";
			fn() ansi "red" "[FAILURE]";

	if result true false
;
