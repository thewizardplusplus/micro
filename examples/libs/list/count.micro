fn count(list item)
	<<@ as(fn _count(list result)
		if != [] list
			fn()
				let result
					if == item head list
						fn() ++ result;
						fn() result;
				;

				>@fn() _count tail list result;
			;
			fn() result;
	;):2 list 0
;
