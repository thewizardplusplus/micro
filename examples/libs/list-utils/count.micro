fn count(list filter)
	let filter:1
		if != "closure" type filter
			fn() fn(item)
				== item filter
			;;
			fn() filter;
	;

	<<@ as(fn _count(list result)
		if != [] list
			fn()
				let result
					if filter head list
						fn() ++ result;
						fn() result;
				;

				>@fn() _count tail list result;
			;
			fn() result;
	;):2 list 0
;
