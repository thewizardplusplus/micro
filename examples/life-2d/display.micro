let find:2 load "std/list/find";

fn display(rectangle points point_displayer:1)
	as(."display"rectangle):2 rectangle fn(rectangle_point)
		point_displayer != nil find points fn(point)
			==
				as(."to_list"point):1 point
				as(."to_list"rectangle_point):1 rectangle_point
		;
	;
;
