fn Point(x y)
	#"x" x
	#"y" y

	#"translate" fn(this other)
		Point + ."x"this ."x"other + ."y"this ."y"other
	;

	#"from_list" fn(this list)
		Point head list head tail list
	;

	#"to_list" fn(this)
		, ."x"this , ."y"this []
	;
{};
