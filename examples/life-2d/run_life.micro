let while:3 load "std/while";

let display:3 load "display";
let populate:1 load "populate";

fn run_life(rectangle points point_displayer)
	while points fn() true; fn(points)
		display rectangle points point_displayer
		outln ""

		populate points
	;
;
