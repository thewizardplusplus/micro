let Point load "Point";
let BaseRectangle:3 load "../common/BaseRectangle";

fn Rectangle(minimum maximum)
	+BaseRectangle minimum maximum Point

	#"get_random_axis" fn(this start end)
		+ start * random - end start
	;

	#"get_random_point" fn(this)
		as(Point):2
			as(."get_random_axis"this):3 this ."x"."minimum"this ."x"."maximum"this
			as(."get_random_axis"this):3 this ."y"."minimum"this ."y"."maximum"this
	;
{};
