let test:2 load "../libs/test/test";
let group:2 load "../libs/test/group";

group "functional tests" fn()
	,test "use of a hash table as an object" fn()
		fn Adder(sum)
			#"sum" sum

			#"set_sum" fn(this sum)
				#"sum" sum
			this;

			#"add" fn(this number)
				let sum + ."sum"this number;
				as(."set_sum"this):2 this sum
			;
		{};

		let adder Adder 2;
		let adder as(."add"adder):2 adder 3;
		== 5 ."sum"adder
	;
[];
