let utils load "test_utils";

as(."test"utils):2 "bitwise AND" fn()
	== 8 & 14 9
;

as(."test"utils):2 "bitwise OR" fn()
	== 15 | 14 9
;

as(."test"utils):2 "bitwise XOR" fn()
	== 7 ^ 14 9
;

as(."test"utils):2 "bitwise left shift" fn()
	== 36 << 9 2
;

as(."test"utils):2 "bitwise arithmetic right shift" fn()
	&&
		== 2 >> 9 2
		== _3 >> _9 2
;

as(."test"utils):2 "bitwise logical right shift" fn()
	&&
		== 2 >>> 9 2
		== 1073741821 >>> _9 2
;

as(."test"utils):2 "bitwise NOT" fn()
	== _10 ~ 9
;
