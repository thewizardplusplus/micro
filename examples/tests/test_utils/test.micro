let ansi load "ansi";

fn test(name test)
	let result test;
	outln
		+ as(ansi):2 "bold" name
		+ " "
		if result
			fn() as(ansi):2 "green" "[SUCCESS]";
			fn() as(ansi):2 "red" "[FAILURE]";

	if result 1 0
;
