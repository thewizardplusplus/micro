fn reduce(initial list handler:2)
	<<@ as(fn _reduce(list result)
		if != [] list
			fn()
				let result handler result head list;
				>@fn() _reduce tail list result;
			;
			fn() result;
	;):2 list initial
;
